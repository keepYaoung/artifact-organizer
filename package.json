{
  "name": "artifact-organizer",
  "private": true,
  "version": "0.5.2-alpha",
  "description": "Agent-driven visual output — emit semantic JSON, get self-contained HTML with zero dependencies",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "node --test tests/**/*.test.mjs tests/*.test.mjs",
    "test:update-golden": "UPDATE_GOLDEN=1 node --test tests/render.test.mjs",
    "build:catalog": "node tools/build-catalog-md.mjs"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/keepYaoung/artifact-organizer.git"
  },
  "keywords": ["a2ui", "visual-explainer", "claude-code", "generative-ui", "llm"]
}
